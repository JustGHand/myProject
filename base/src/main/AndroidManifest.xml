<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pw.codeset">

    <application
        android:name=".application.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".activity.MainActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--贝塞尔曲线-->
        <activity android:name=".activity.BezierCurveActivity">

        </activity>

        <!--触摸view-->
        <activity android:name=".activity.TouchViewAct">

        </activity>
        <!--mvp架构登录页面-->
        <activity android:name=".activity.login_mvp.UserLoginActivity">

        </activity>
    </application>

</manifest>