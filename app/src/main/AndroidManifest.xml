<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pw.codeset">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:name=".application.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".activity.main.MainActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--贝塞尔曲线-->
        <activity android:name=".activity.bezier.BezierCurveActivity">

        </activity>

        <!--触摸view-->
        <activity android:name=".activity.touchview.TouchViewAct">

        </activity>
        <!--mvp架构登录页面-->
        <activity android:name=".activity.login_mvp.UserLoginActivity">

        </activity>
        <!--游戏列表-->
        <activity android:name=".activity.games.GamesActivity">

        </activity>
        <!--黑白格-->
        <activity android:name=".activity.games.block.BlockGameActivity"
            android:windowSoftInputMode="adjustResize"
            >

        </activity>

        <activity android:name=".activity.pc_connect.ActivityPcConnect"
            />

        <activity android:name=".activity.notes.NotesActivity" />
        <activity android:name=".activity.notes.NotesEditActivity" />
        <activity android:name=".activity.read.bookshelf.BookShelfActivity" />


        <activity android:name=".activity.read.leadbook.LeadBookActivity" />

        <service android:name=".activity.read.leadbook.wifitransfer.WebService"
            android:enabled="true"
            android:exported="true"
            tools:ignore="WrongManifestParent"></service>

        <activity android:name=".activity.read.read.ReadActivity" />
    </application>

</manifest>