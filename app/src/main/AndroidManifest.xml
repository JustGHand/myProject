<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pw.codeset">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:name=".application.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".abilities.main.MainActivityView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--贝塞尔曲线-->
        <activity android:name=".abilities.bezier.BezierCurveActivity">

        </activity>

        <!--触摸view-->
        <activity android:name=".abilities.touchview.TouchViewAct">

        </activity>
        <!--mvp架构登录页面-->
        <activity android:name=".abilities.login_mvp.UserLoginActivity">

        </activity>
        <!--游戏列表-->
        <activity android:name=".abilities.games.GamesActivity">

        </activity>
        <!--黑白格-->
        <activity android:name=".abilities.games.block.BlockGameActivity"
            android:windowSoftInputMode="adjustResize"
            >

        </activity>

        <activity android:name=".abilities.pc_connect.ActivityPcConnect"
            />

        <activity android:name=".abilities.notes.NotesActivity" />
        <activity android:name=".abilities.notes.NotesEditActivity" />
        <activity android:name=".abilities.read.bookshelf.BookShelfActivity" />


        <activity android:name=".abilities.read.leadbook.LeadBookActivity" />

        <service android:name=".abilities.read.leadbook.wifitransfer.WebService"
            android:enabled="true"
            android:exported="true"
            tools:ignore="WrongManifestParent"></service>

        <activity android:name=".abilities.read.read.ReadActivity"/>

        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />
        <meta-data
            android:name="android.notch_support"
            android:value="true" />
    </application>

</manifest>